@model ReportWebApp01.Models.Employee

@{
    ViewBag.Title = "RadioList";
}

<h2>RadioList</h2>

@Html.RadioButtonListFor(model => model.Nickname,
    (IEnumerable<SelectListItem>)ViewBag.Names,
    new { @class = "radio inline" })

